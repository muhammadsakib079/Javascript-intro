// Rule ----- 1. Create two arrays of your choice.
const firstArray = ['apple', 'banana', 'orange'];

// Rule ----- 2. Use the concat method to combine the two arrays into a new array.
const secondArray = ['grapes', 'kiwi', 'mango'];

// Rule ----- 3. Print both the original arrays and the combined array using console.log().
console.log(firstArray.concat(secondArray));