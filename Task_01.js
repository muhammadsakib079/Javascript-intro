// Rule ----- 1. Declare an array with 5 elements containing fruits.
const fruits = ['apple', 'banana', 'orange', 'grapes', 'mango'];

// Rule ----- 2. Console log the 3rd index element.
console.log(fruits[3]);

// Rule ----- 3. Change the value of the 2nd index element to jambura.
fruits[2] = 'jambura';

// Rule ----- 4. console log the final array.
console.log(fruits);